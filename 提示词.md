# 任务：初始化 Taro3 + React + TypeScript 基础脚手架
## 上下文
当前目录是git仓库，使用pnpm包管理器，目标是微信小程序+H5多端项目。只搭建基础框架，不写业务代码。

## 要求
1. 技术栈：Taro3 + React + TypeScript，样式使用 scss
2. TS 严格模式，禁止随意使用 any
3. 目录遵循 Taro3 官方规范：
   - src/pages/index 首页
   - src/components 目录预留，放公共组件
4. 生成并配置：
   - package.json，配置脚本：dev:weapp、build:weapp、dev:h5、build:h5、tsc、lint
   - tsconfig.json
   - eslint + prettier 基础配置
   - app.config.ts，注册首页路由
   - .gitignore，忽略 node_modules、dist 等产物
5. 不要引入额外UI库、多余依赖。

## 验收标准
1. pnpm install 安装依赖无报错
2. pnpm dev:weapp 可正常编译微信小程序
3. pnpm dev:h5 可正常启动H5开发服务
4. pnpm tsc 类型检查无报错

## 输出规则
1. 先列出将要创建/修改的所有文件清单
2. 展示每个文件diff，等待我确认后再写入磁盘
3. 全部文件生成完成后，执行 pnpm install && pnpm tsc，返回命令完整输出
4. 最后输出完整项目目录树

---

# 任务：配置 Taro3 支持 RN、微信小程序、H5、支付宝、抖音多端
## 上下文
已有 Taro3 + React + TS 脚手架（Taro 3.6.40），平台插件 weapp/alipay/tt/h5 已装，缺 RN 依赖。

## 修改内容
1. package.json：dependencies 增加 react-native ^0.69.9，devDependencies 增加 @tarojs/rn-runner 3.6.40
2. config/index.ts：outputRoot 改为 dist/${process.env.TARO_ENV || 'h5'}，按端隔离产物
3. 执行 pnpm install 安装依赖

## 验收
- pnpm dev:weapp / dev:h5 / build:alipay / build:tt / build:rn 可正常编译

---

# 任务：生成 RN 壳工程
## 要求
1. 克隆 Taro 官方壳工程 taro-native-shell@v0.69.7-3 到 rn-shell/（通过 ghfast.top 镜像，直连 GitHub 失败）
2. appName 统一改为 taroExam（主项目 config/index.ts、壳工程 config/index.js、android settings.gradle/strings.xml/MainActivity、ios AppDelegate.mm/Info.plist）
3. 壳工程用 pnpm 安装：.npmrc 加 node-linker=hoisted（避免 Metro 不识别 pnpm 软链）
4. 主项目 react-native 锁 0.69.7 与壳工程对齐；.gitignore 加 rn-shell/；删除壳工程嵌套 .git
5. 回滚外部误改的 Taro 4.x RN 依赖（taro-rn 4.2.1/RN 0.70/expo 47），恢复 3.6.40 + RN 0.69.7

## 遗留
- Node v24 下 build:rn 报 spawn EINVAL（Node 18.20.2+ 安全修复禁止 spawn .cmd），需 Node 16/18
- iOS 需 Mac + pod install；Android 需本机 Android SDK + JDK 11+

---

# 任务：题目一 严格还原视觉稿
## 上下文
使用 mock 数据模拟地址列表，重点实现第三行：标签字数不固定；标签可在开头或第二行结尾；结尾标签在第二行时最宽占 50%；地址最多两行；兼容 RN、微信、H5、支付宝、抖音。

## 实现要点
1. 新增 AddressItem / Tag 组件及工具函数 measure / truncate。
2. 用字符宽度估算统一处理跨端文本测量。
3. JS 手动截断地址，保证地址 + 结尾标签最多 2 行，结尾标签宽度不超过 50%。
4. 样式使用 rpx，按 750 设计稿还原。

## 验收
- npx tsc --noEmit 通过
- npm run build:weapp / dev:h5 / build:rn 可编译
- 各端地址两行省略、尾标签 50% 宽度表现一致

---

# 任务：配置两个 tab 页
## 上下文
将当前页面改为「题目一」tab，新增「题目二」tab。

## 修改内容
1. `src/app.config.ts`：pages 改为 `pages/question-one/index`、`pages/question-two/index`，新增 tabBar 配置。
2. 删除 `src/pages/index/`，迁移为 `src/pages/question-one/`（题目一地址列表）。
3. 新建 `src/pages/question-two/`（题目二占位页）。

## 验收
- npx tsc --noEmit 通过
- npm run build:weapp / build:h5 / build:rn / build:alipay / build:tt 可编译
- 小程序/H5 底部显示「题目一」「题目二」两个 tab

---

# 任务：题目二 优惠中心浮层动效
## 上下文
在「题目二」页面增加「优惠中心」入口按钮，点击后从底部滑出优惠券浮层，严格还原参考视频动效。

## 具体要求
1. 浮层内展示优惠券列表，支持上下滑动。
2. 点击「每月领券」券包的领券按钮后，暴涨 1~4 张新券。
3. 每个新券对应一个红包，沿抛物线飞向该新券商品图的正中间。
4. 红包落地后，新券背景高亮闪一闪。
5. 显示 Toast「成功领取 N 张券」，底部按钮状态从「一键领券」→「领券中...」→「再看一次」。
6. 兼容 RN、微信、H5、支付宝、抖音，多端表现尽可能一致。

## 实现要点
1. 新增 `CouponCenter`、`CouponCard`、`RedPacket`、`CouponTabs`、`CouponList` 等组件。
2. 用统一 JS 动画参数（贝塞尔曲线、时长、缓动）+ 平台适配渲染（Web inline-transform / RN Animated）。
3. 新券数据用 mock，点击「再看一次」可重新触发整套动画。
4. 样式继续遵循 Taro3 RN 兼容写法：rpx、拆解 shorthand、无 inline/inline-block。

## 验收
- npx tsc --noEmit 通过
- npm run build:weapp / build:h5 / build:rn / build:alipay / build:tt 可编译
- 红包抛物线落点位于新券图正中心
- 新券高亮闪一闪、Toast、底部按钮状态变化正常


---

# 子任务：Task 3 CouponTag 组件
## 要求
1. 新建 `src/components/CouponTag/index.tsx` 与 `index.scss`。
2. 消费 `CouponTag` 类型（text / color? / bgColor?），产出 `<CouponTag text variant />`。
3. 默认色 color=#ff4d4f、bgColor=#fff2f0，inline style 设置颜色。
4. 样式 RN 兼容：padding / border-radius 拆解四边，rpx 单位。
5. npx tsc --noEmit 与 npm run build:weapp 均通过后提交。

---

# 子任务：Task 4 CouponCard 组件
## 要求
1. 新建 `src/components/CouponCard/index.tsx` 与 `index.scss`。
2. 消费 `CouponItem` 类型与 `CouponTag` 组件，产出 `<CouponCard item onClaim ref />`。
3. 使用 forwardRef / useImperativeHandle 暴露 `{ id, getImageRef }`，供后续坐标测量任务使用。
4. 商品图用 CSS/Emoji 占位（🍔），不引入二进制图片资源。
5. 已领取券显示旋转 30deg 的「已领取」水印（RN 告警可接受）。
6. 样式 RN 兼容：padding / border / border-radius 拆解四边，rpx 单位。
7. npx tsc --noEmit 与 npm run build:weapp 均通过后提交。

---

# 子任务：Task 5 CouponTabs 组件
## 要求
1. 新建 `src/components/CouponTabs/index.tsx` 与 `index.scss`。
2. 产出 `<CouponTabs active='领券' />`，顶部选项卡为纯展示组件，不支持切换。
3. 固定四个 Tab：我的优惠券 / 付费会员 / 代金券 / 领券；active 项加粗变色并显示底部红色指示条。
4. 样式 RN 兼容：border / border-radius 拆解四边，rpx 单位，flex 布局。
5. npx tsc --noEmit 与 npm run build:weapp 均通过后提交。

---

# 子任务：Task 6 CouponList 组件
## 要求
1. 新建 `src/components/CouponList/index.tsx` 与 `index.scss`。
2. 消费 `CouponItem[]`、`CouponCard`、`onClaim`，产出 `<CouponList coupons onClaim />`。
3. 使用 forwardRef 包裹 ScrollView（scrollY），逐项渲染 CouponCard，底部加 40rpx 安全区占位 View。
4. 样式：flex:1，背景色 #f5f5f5，rpx 单位。
5. npx tsc --noEmit 与 npm run build:weapp 均通过后提交。

---

# 子任务：Task 7 基础 CouponCenter 容器与入口按钮
## 要求
1. 新建 `src/components/CouponCenter/index.tsx` 与 `index.scss`，消费 `useCouponCenter`、`CouponTabs`、`CouponList`，产出 `<CouponCenter visible onClose />`。
2. 重构 `useCouponCenter`：移除 `isOpen`/`open`/`close`，由页面通过 `visible`/`onClose` 控制弹层显隐。
3. 弹层结构：遮罩（点击关闭）、底部 80vh 面板、头部标题+关闭按钮、Tabs、CouponList、底部红色圆角按钮（footerText）。
4. 修改 `src/pages/question-two/index.tsx` 与 `index.scss`：页面居中放置「优惠中心」入口按钮，点击打开弹层。
5. 样式 RN 兼容：padding / border / border-radius 拆解四边，rpx 单位，flex 布局。
6. npx tsc --noEmit、npm run build:weapp、npm run dev:h5 均通过后提交。

# 子任务：Task 8 坐标测量 Hook
## 要求
1. 新建 `src/components/CouponCenter/hooks/useMeasurements.ts`，消费 `Point` 类型与 `isRN`，产出 `measurePositions(packageId, couponIds): Promise<FlyingPacketInit[]>`。
2. Web 端使用 `Taro.createSelectorQuery()` 配合 id 选择器（`#coupon-center-sheet`、`#claim-btn-${packageId}`、`#coupon-img-${id}`）测量起止坐标，坐标相对弹层面板。
3. RN 分支先返回空数组兜底（Task 10/13 再实现）。
4. npx tsc --noEmit、npm run build:weapp、npm run build:rn 均通过后提交。

---

# 子任务：Task 11 集成爆炸动画
## 要求
1. 修改 `src/components/CouponCard/index.tsx`：给商品图 View 加 `id={`coupon-img-${item.id}`}`，给领券按钮加 `id={`claim-btn-${item.id}`}`。
2. 修改 `src/components/CouponCenter/hooks/useCouponCenter.ts`：引入 `useAnimation` 与 `useMeasurements`；点击券包时生成新券、setTimeout 100ms 后测量坐标、设置 Toast、调用 `startExplosion`；动画 done 后 500ms 清除 `isNew` 标记。
3. 修改 `src/components/CouponCenter/index.tsx`：引入 `RedPacket`，渲染红包层与 Toast；`CouponList.onClaim` 传给 `claimPackage`。
4. 修改 `src/components/CouponCenter/index.scss`：新增 `.coupon-center__packets` 与 Toast 样式；实现中把 Toast 的 CSS `transform: translate(-50%, -50%)` 改为外层 flex 居中，避免 RN 构建报错。
5. npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 均通过后提交。

---

# 子任务：Task 9 RedPacket 组件
## 要求
1. 新建 `src/components/RedPacket/index.tsx` 与 `index.scss`。
2. 消费 `FlyingPacket`、`isRN`，产出 `<RedPacket packet progress />`。
3. 通过 `quadraticBezierPoint` 计算贝塞尔曲线位置，内联 transform（translate + scale）与 opacity 渲染飞行红包粒子；RN 分支暂用相同内联 transform（Task 12 替换为 Animated）。
4. 样式：64rpx 容器 flex 居中、pointer-events: none、图标 56rpx。
5. npx tsc --noEmit、npm run build:weapp、npm run build:rn 均通过后提交。

---

# 子任务：Task 10 动画控制器 Hook
## 要求
1. 新建 `src/components/CouponCenter/hooks/useAnimation.ts`，消费 `FlyingPacket`、`MeasuredTarget`、`getControlPoint`、`isRN`，产出 `{ state, startExplosion, clear }`（state 含 phase/packets/progress）。
2. 使用 requestAnimationFrame 驱动红包飞行动画：DURATION=700ms、STAGGER=80ms 错峰、easeOutQuad 缓动；phase 流转 idle → exploding → landed → flashing(500ms) → done，完成后回调 onDone。
3. `clear()` 取消当前 RAF（RN 分支跳过 cancelAnimationFrame）。
4. npx tsc --noEmit、npm run build:weapp、npm run build:rn 均通过后提交。

---

# 子任务：Task 12 RedPacket RN Animated 分支
## 要求
1. 修改 `src/components/RedPacket/index.tsx`：RN 分支替换为 React Native Animated（`Animated.Value` + `Animated.timing` 700ms、`Easing.out(Easing.quad)`、`useNativeDriver: true`），translateX/translateY 通过 `interpolate` 从 start 到 end，Web 分支保持贝塞尔内联 transform 不变。
2. 注意：`react-native` 入口含 Flow 语法，web 端 webpack 无法解析，不能顶层 import；改用 `process.env.TARO_ENV === 'rn'` 编译期常量守卫的 `require('react-native')`，类型用 `typeof import('react-native')` 保持严格类型。
3. npx tsc --noEmit、npm run build:weapp、npm run build:rn 均通过后提交。

---

# 子任务：Task 13 RN Measurement Branch
## 要求
1. 修改 `src/components/CouponCenter/hooks/useMeasurements.ts` 的 RN 分支。
2. 因 RN 端通过 refs/UIManager.measureLayout 实现精确测量较复杂，本任务改为实现基于标准屏宽的 rpx→dp 估算兜底：按 1rpx≈0.5dp 给出红包起点与每个新券图终点的坐标。
3. npx tsc --noEmit、npm run build:rn 均通过后提交。

---

# 子任务：Task 14 最终验证与收尾
## 要求
1. 全量构建矩阵：npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 全部通过。
2. 视觉验证：H5 dev server 与微信开发者工具检查浮层、券列表、红包抛物线、高亮闪烁、Toast 与底部按钮状态；RN 检查红包出现且无红屏/黄屏。
3. 追加 提示词.md 完成记录。
4. 最终提交（排除未跟踪的 References/frames 参考资料）。

---

# 子任务：视觉验证后修改（延时领取 / 落点居中 / 红包消失 / 新券闪烁 / 印章居中）
## 要求
1. 点击券包「领券」按钮后，该按钮立即变灰显示「领券中...」，2 秒后再插入新券并执行红包动画（不是底部按钮显示）。
2. 分裂后红包的抛物线落点必须落在券图正中间：红包元素左上角对齐轨迹点导致视觉偏右下半个尺寸，通过负 margin 半尺寸偏移（web: -32rpx；RN: -16dp + 固定 32dp 尺寸）让红包中心对准券图中心。
3. 动画结束（落袋 landed）后红包立即消失：clear packets 数组。
4. 新领到的券高亮闪一闪：useAnimation 新增 flashOn 节拍，flashing 阶段亮/灭交替 3 次（200ms 间隔，纯 JS 驱动兼容 RN），经 CouponCenter → CouponList → CouponCard 透传控制 coupon-card--new 类。
5. 「已领取」印章下移垂直居中：外层包 absolute 全高 flex 容器 justify-content: center，避免 translateY(-50%) 的 RN 兼容风险。
6. 顺带清理：useAnimation 的 setTimeout 链纳入 clear() 取消；useCouponCenter 计时器卸载时清理。
7. npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 均通过后提交。
8. 追加：重复领取时，上一轮领到的新券卡片先消失（按 `${packageId}-new-` 前缀过滤），再插入本轮新券，不再累积。

---

# 子任务：领取交互与列表卡片化微调
## 要求
1. 「领券中...」按钮不置灰，保持红色，仅切换文案并禁用点击。
2. 领券完成（动画 done）后，「每月领券」券包卡片从列表中消失（claimedPkgRef 记录，done 时过滤移除）。
3. 领券 tab 的券列表区域：浅灰背景加内边距（24rpx），每张券卡片圆角 16rpx、卡片间距 margin-bottom 24rpx，移除原分隔线与券包卡片边框。
4. npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 均通过后提交。
5. 追加：券列表容器 `.coupon-list` 增加 `box-sizing: border-box`，防止 24rpx 内边距撑出容器（RN 默认 border-box，该属性被忽略，无影响）。

---

# 子任务：卡片按钮对齐与印章微调
## 要求
1. 「每月领券」券包的领取按钮垂直居中：action 列改为 stretch 全高 + flex column，券包修饰类 justify-content: center；其他卡片的按钮底部对齐（justify-content: flex-end）。
2. 新券卡片重复显示两个「新品尝鲜」：删除 mock 数据中的 tags（顶部标签），保留下方 subtitle。
3. 「立即使用」按钮不置灰，改红色 #d6001c，底部对齐（随 action 列 flex-end）。
4. 「已领取」印章放大（80rpx→100rpx，字号 20→24rpx）并上移（居中容器内 margin-bottom 40rpx）。
5. npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 均通过后提交。

---

# 子任务：按钮可用性与底部一键领券
## 要求
1. 仅「每月领券」券包卡片的领券按钮可点击，其他卡片的领券按钮无操作（onClick 仅券包挂载）。
2. 底部「一键领券」按钮修复展示并可用：原 Text 内联元素宽高易失效，改为 View + flex 居中（__footer-btn 容器 + __footer-text 纯文字样式）；点击 = 等同点击券包「领券」（取列表中 isPackage 卡片 id 调 claimPackage），状态一致，券包按钮同步显示「领券中...」。
3. 所有领券按钮宽度调大为四个汉字宽度：box-sizing: border-box + width 144rpx（24rpx 字号 × 4 + 左右各 24rpx padding）+ text-align center，「领券」「领券中...」「立即使用」「不可用」均不换行不错位。
4. npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 均通过后提交。

---

# 子任务：动画前移除券包 / 标题两行省略 / 虚线规则区 / 底部按钮修复
## 要求
1. 一键领券按钮仍显示不全：根因是 ScrollView flex:1 的 min-height:auto 被内容撑高把 footer 顶出 80vh 浮层，`.coupon-list` 增加 `min-height: 0`。
2. 点击领券后，动画播放前券包卡片就消失：useMeasurements 拆为 measureStart（先测按钮起点，卡片还在）+ measureEnds（新券渲染后测落点）；2s 后先测起点 → 同帧移除券包并原位插入新券 → 测落点 → 播放动画，落点坐标与最终布局一致。
3. 卡片标题最多两行：numberOfLines={2}（RN）+ -webkit-line-clamp: 2（H5/小程序），-webkit-box 块级化同时修复第一行不到最右就折行。
4. 规则说明一行显示并上加分隔虚线：reason 移出内容列，卡片改 flex column，新增 __main 行 + 全宽 __rule 区（border-top dashed），立即使用按钮位于虚线上方主行内；reason numberOfLines={1} + line-clamp: 1。
5. npx tsc --noEmit、npm run build:weapp / h5 / rn / alipay / tt 均通过后提交。
6. 追加：已领取印章上移避免与「立即使用」按钮重叠——watermark-wrap 移入 __main 主行（__main 加 position: relative），只在虚线以上区域垂直居中，right 改 0 与按钮右缘对齐。
7. 追加：一键领券按钮固定弹窗底部可见——footer 改 position: absolute 钉在 sheet 底部（z-index 2 白底不透明），sheet 加 overflow: hidden 兜底，`.coupon-list` 加 margin-bottom: 112rpx 给 footer 让位，不再依赖 flex 不被内容顶出。
